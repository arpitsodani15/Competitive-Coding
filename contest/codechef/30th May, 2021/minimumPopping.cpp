#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int t;
    cin >> t;
    while(t--){
        int n, m;
        cin >> n >> m;
        map<int, int> cnt;
        deque<int> q;
        int uni = 0;
        for(int i=0; i<m; i++){
            int x;
            cin >> x;
            q.push_back(x);
        }
        while(cnt.size()!=n)
    }
    return 0;
}
